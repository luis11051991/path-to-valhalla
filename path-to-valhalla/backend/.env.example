# Copia este archivo a .env.local (base local) o .env.server (base remota ZeroTier)
# y rellena los valores reales. NUNCA subas credenciales reales a git.
DB_HOST=127.0.0.1
DB_PORT=5432
DB_NAME=nombre_base_datos
DB_USER=usuario
DB_PASSWORD=contrasena
DB_SSL=false
